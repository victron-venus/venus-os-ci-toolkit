name: 'Auto Approve'
description: 'Auto-approve PRs opened by configured authors'

on:
  workflow_call:
    inputs:
      pr-author:
        description: 'Comma-separated PR authors to auto-approve (exact login, no spaces)'
        type: string
        default: 'dependabot[bot],renovate[bot],4alvit'
      required-reviews:
        description: 'Unused, kept for caller compatibility'
        type: string
        default: '1'
      pr-types:
        description: 'Unused, kept for caller compatibility'
        type: string
        default: 'dependencies'
    secrets:
      BOT_PAT:
        description: 'GitHub token (BOT_PAT) for approving PRs; falls back to github.token'
        required: false

jobs:
  auto-approve:
    name: Auto Approve
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    env:
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: "true"
    steps:
      - name: Auto Approve
        # auto-approve-action v4 cannot filter by author/type, so gate here.
        # Step-level (not job-level): inputs context is unreliable in a called
        # job's if and fails workflow validation at startup.
        if: contains(split(inputs.pr-author, ','), github.actor)
        uses: hmarr/auto-approve-action@f0939ea97e9205ef24d872e76833fa908a770363
        with:
          github-token: ${{ secrets.BOT_PAT || github.token }}
