# Retired publisher/dispatcher. Historical implementation: docs/legacy-workflows/nightly.yml.txt
name: Retired legacy nightly
'on':
  workflow_call:
    inputs:
      cron:
        description: Cron schedule for nightly build
        type: string
        default: 0 2 * * *
      workflow-to-trigger:
        description: Workflow file to trigger (e.g., ci.yml, docker-build.yml)
        type: string
        required: true
      workflow-inputs:
        description: JSON string of inputs to pass to triggered workflow
        type: string
        default: '{}'
permissions:
  contents: read
jobs:
  migration-required:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
    - name: Use the gated release policy
      run: 'echo "This legacy entry point is retired. Install the reviewed release policy and build-only adapter;
        see docs/release-workflow.md and docs/FLEET_ROLLOUT.md." >&2

        exit 1

        '
