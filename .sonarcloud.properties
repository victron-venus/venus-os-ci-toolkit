# Confirmed S8707 false positives: validated values are document contents, while
# PR-body and release-manifest destinations are fixed internal paths.
# Automatic analysis supports file exclusions; these two files are intentionally
# excluded with repository-owner approval. Keep all other release code analyzed.
sonar.exclusions=scripts/prepare_version.py,scripts/release_versioned.py
